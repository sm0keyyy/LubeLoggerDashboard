<UserControl x:Class="LubeLoggerDashboard.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LubeLoggerDashboard.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel Margin="20">
            <TextBlock Text="Reports" FontSize="24" Margin="0,0,0,20"/>
            <TextBlock Text="View reports and analytics" FontSize="18" Margin="0,0,0,20"/>
            
            <!-- Placeholder for reports -->
            <TabControl>
                <TabItem Header="Cost Analysis">
                    <Border Background="#f5f5f5" CornerRadius="5" Padding="15" Margin="0,10,0,10">
                        <StackPanel>
                            <TextBlock Text="Cost Analysis" FontWeight="Bold" Margin="0,0,0,10"/>
                            <TextBlock Text="No cost data available." Foreground="#7f8c8d"/>
                            <Button Content="Generate Report" Margin="0,10,0,0" Padding="10,5" Background="#3498db" Foreground="White"/>
                        </StackPanel>
                    </Border>
                </TabItem>
                <TabItem Header="Service Frequency">
                    <Border Background="#f5f5f5" CornerRadius="5" Padding="15" Margin="0,10,0,10">
                        <StackPanel>
                            <TextBlock Text="Service Frequency" FontWeight="Bold" Margin="0,0,0,10"/>
                            <TextBlock Text="No service frequency data available." Foreground="#7f8c8d"/>
                            <Button Content="Generate Report" Margin="0,10,0,0" Padding="10,5" Background="#3498db" Foreground="White"/>
                        </StackPanel>
                    </Border>
                </TabItem>
                <TabItem Header="Maintenance Prediction">
                    <Border Background="#f5f5f5" CornerRadius="5" Padding="15" Margin="0,10,0,10">
                        <StackPanel>
                            <TextBlock Text="Maintenance Prediction" FontWeight="Bold" Margin="0,0,0,10"/>
                            <TextBlock Text="No maintenance prediction data available." Foreground="#7f8c8d"/>
                            <Button Content="Generate Report" Margin="0,10,0,0" Padding="10,5" Background="#3498db" Foreground="White"/>
                        </StackPanel>
                    </Border>
                </TabItem>
            </TabControl>
        </StackPanel>
    </Grid>
</UserControl>