<UserControl x:Class="LubeLoggerDashboard.UI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LubeLoggerDashboard.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Reports" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBlock Text="View and generate reports" FontSize="16" Margin="0,0,0,20"/>
            <TextBlock Text="This is a placeholder for the reports view. In a real application, this would allow generating various reports about vehicle maintenance, costs, and usage." 
                       TextWrapping="Wrap" Margin="0,0,0,20"/>
            
            <!-- Placeholder for report types -->
            <Border Background="#f5f5f5" Padding="15" CornerRadius="5" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Available Reports" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel Margin="10,0,0,0">
                        <TextBlock Text="• Maintenance History" Margin="0,0,0,5"/>
                        <TextBlock Text="• Cost Analysis" Margin="0,0,0,5"/>
                        <TextBlock Text="• Fuel Efficiency" Margin="0,0,0,5"/>
                        <TextBlock Text="• Service Schedule" Margin="0,0,0,5"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- Placeholder for report preview -->
            <Border Background="#f5f5f5" Padding="15" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Report Preview" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Select a report type to generate a preview" FontStyle="Italic" Foreground="#7f8c8d"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>