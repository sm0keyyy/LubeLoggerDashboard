<UserControl x:Class="LubeLoggerDashboard.UI.Views.MaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LubeLoggerDashboard.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Maintenance" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBlock Text="Track and manage vehicle maintenance" FontSize="16" Margin="0,0,0,20"/>
            <TextBlock Text="This is a placeholder for the maintenance management view. In a real application, this would display maintenance records, schedules, and allow adding new maintenance entries." 
                       TextWrapping="Wrap" Margin="0,0,0,20"/>
            
            <!-- Placeholder for maintenance records -->
            <Border Background="#f5f5f5" Padding="15" CornerRadius="5" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Maintenance Records" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="No maintenance records found" FontStyle="Italic" Foreground="#7f8c8d"/>
                </StackPanel>
            </Border>
            
            <!-- Placeholder for maintenance schedule -->
            <Border Background="#f5f5f5" Padding="15" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Upcoming Maintenance" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="No scheduled maintenance" FontStyle="Italic" Foreground="#7f8c8d"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>